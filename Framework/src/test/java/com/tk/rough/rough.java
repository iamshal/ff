package com.tk.rough;

import java.util.ArrayList;
import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;

public class rough {
/*	// driver.findElement(By.xpath("//a[@ng-click='triggerSummaryJob()'][text()='Start
			// the job to calculate summary']")).click();

			// to catch all web elements into list
		//	List<WebElement> myList = driver
//					.findElements(By.xpath("//SPAN[@class='data-type'][text()='String'][text()='String']"));

			// myList contains all the web elements
			// if you want to get all elements text into array list
			List<String> all_elements_text = new ArrayList<>();

			for (int i = 0; i < myList.size(); i++) {

				// loading text of each element in to array all_elements_text
				all_elements_text.add(myList.get(i).getText());

				// to print directly
				System.out.println(myList.get(i).getText());

			}
			
			WebElement e=driver.findElement(By.xpath("//TD[text()='9']"));
			System.out.println(e.getSize());
			driver.findElement(By.xpath("(//a[text()='Show Distribution'])[3]")).click();

			
			/*
			 * //to catch all web elements into list List<WebElement>
			 * myList1=driver.findElements(By.xpath(""));
			 * 
			 * //myList contains all the web elements //if you want to get all
			 * elements text into array list List<String> all_elements_text1=new
			 * ArrayList<>();
			 * 
			 * for(int i=0; i<myList1.size(); i++){
			 * 
			 * //loading text of each element in to array all_elements_text
			 * all_elements_text1.add(myList1.get(i).getText());
			 * 
			 * //to print directly System.out.println(myList1.get(i).getText());
			 * 
			 * 
			 * 
			 * 
			 * 
			 * 
			 * /* driver.findElement(By.xpath("(//a[text()='Show Distribution'])[3]"
			 * )).click(); // if(string != null // System.out.println(
			 * "String is not null and not empty"); // Read more: //
			 * http://javarevisited.blogspot.com/2016/01/how-to-check-if-string-is-
			 * not-null-and-empty-in-java-example.html#ixzz50f0BRCon
			 * Thread.sleep(5000); driver.findElement(By.xpath(
			 * "//I[@class='fa fa-times-circle-o']")).click();
			 * 
			 * Thread.sleep(2000);

}
*/
}